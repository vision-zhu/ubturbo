# SMAP 发布说明

## 6.0.10

仓库历史记录将 6.0.10 标记为初始发布。由于当前文件没有对应发布日期、标签或问题单，本说明不补写
无法验证的功能和修复列表。

### 公共接口

当前公开接口以 `src/user/smap_interface.h` 为准，包含生命周期、迁出/迁回、进程跟踪、查询和远端
NUMA 迁移能力。详细清单见 [API 参考](docs/api_reference.md)。

### 兼容性

- aarch64 Linux/openEuler。
- 用户态库、内核模块、运行内核和内核开发包必须配套。
- 4K 普通进程与 2M 静态大页虚机在启动时选择，不能在同一实例混用。

## 后续发布要求

每个版本应记录发布日期、Git 标签、RPM 版本、内核兼容范围、API/ABI 变化、配置迁移、功能变化、
修复问题和已知限制。没有发布证据的条目不得以 `N/A` 代替长期维护信息。
